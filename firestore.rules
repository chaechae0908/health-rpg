rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 프로토타입용: Firebase Console에서 배포 후 필요 시 보안 규칙을 강화하세요.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
